# 伴成长App - 后端部署小白指南

## 第一步：准备 DeepSeek API Key

1. 打开 https://platform.deepseek.com/
2. 注册登录 → 点「API Keys」→「创建 API Key」
3. 复制 Key（以 `sk-` 开头，**只显示一次，记得保存**）

---

## 第二步：把代码上传到 GitHub（让 Vercel 能拿到代码）

1. 打开 https://github.com/ → 登录
2. 点右上角 `+` → `New repository`
3. Repository name 填：`banchengzhang-backend`
4. 选 `Public`（免费）或 `Private` 都可以
5. 点 `Create repository`
6. 在电脑终端（Mac 的「终端」App）依次执行：

```bash
# 进入后端代码目录（先把代码保存到这里）
cd /Users/studio/.qclaw/workspace/banchengzhang-backend

# 初始化 Git
git init
git add .
git commit -m "first commit"

# 关联 GitHub 仓库（把 xxx 换成你的用户名）
git remote add origin https://github.com/你的用户名/banchengzhang-backend.git
git push -u origin main
```

> 如果提示要登录 GitHub，按屏幕提示输入用户名和密码（或用 Token）

---

## 第三步：用 Vercel 部署（全自动，点几下就行）

1. 打开 https://vercel.com/ → 点 `Sign Up` → 选 `Continue with GitHub` 登录
2. 登录后点 `Add New...` → `Project`
3. 在列表里找到 `banchengzhang-backend` → 点 `Import`
4. 在 `Environment Variables` 区域，添加两个环境变量：

| Name | Value |
|------|-------|
| `DEEPSEEK_API_KEY` | 你第一步复制的 DeepSeek Key（`sk-`开头） |
| `TRIAL_CODES` | 你自己定的试用码，比如 `try001,try002,try003`（逗号分隔，后面可以随时在 Vercel 后台改） |

5. 点 `Deploy` 按钮，**等 1-2 分钟**
6. 部署成功后，Vercel 会给你一个网址，类似：
   `https://banchengzhang-backend-xxx.vercel.app`
   **这个网址记下来，后面要填到 App 代码里**

---

## 第四步：测试一下能不能用

打开终端，执行（把网址换成你自己的）：

```bash
curl -X POST https://你的网址.vercel.app/api/chat \
  -H "Content-Type: application/json" \
  -d '{"trialCode":"try001","messages":[{"role":"user","content":"你好"}],"model":"deepseek-chat"}'
```

如果返回一段文字（AI 的回复），说明**成功了** ✅

如果报错，把错误信息发给我，我帮你看。

---

## 常见问题

**Q: GitHub 不会用怎么办？**
A: 可以跳过 GitHub，直接用 Vercel CLI 部署。先装 Vercel CLI：`npm i -g vercel`，然后执行 `vercel --prod` 按提示操作。

**Q: 试用码怎么改？**
A: 在 Vercel 后台 → 你的项目 → Settings → Environment Variables → 找到 `TRIAL_CODES` → Edit → 改完点 Redeploy。

**Q: DeepSeek 额度用完了怎么办？**
A: 去 https://platform.deepseek.com/ 充值，或者换一个 API Key。在 Vercel 后台改 `DEEPSEEK_API_KEY` 的值就行。

**Q: 想加更多模型（比如通义千问）怎么办？**
A: 改 `pages/api/chat.ts` 里的代码，根据 `model` 参数路由到不同 API。后面需要时告诉我，我帮你改。
